services:
  # REVERSE PROXY (Gère le HTTPS et les ports 80/443)
  caddy:
    image: caddy:2-alpine
    container_name: acd-proxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    deploy:
      resources:
        limits:
          memory: 80M # Très léger
    depends_on:
      - app

  # TON APP NUXT 4
  app:
    # Image pré-construite par le CI et publiée sur GHCR — zéro build sur le serveur
    image: ${APP_IMAGE:-ghcr.io/festival-mmi-2026/acd-app:latest}
    pull_policy: always
    container_name: acd-app
    restart: unless-stopped
    env_file:
      - .env
    # Plus besoin d'exposer le port 3001 au public, Caddy s'en occupe en interne
    expose:
      - "3000"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL}
      GITHUB_CLIENT_ID: ${GITHUB_CLIENT_ID}
      GITHUB_CLIENT_SECRET: ${GITHUB_CLIENT_SECRET}
      GOTENBERG_URL: http://gotenberg:3000
      SMTP_HOST: ${SMTP_HOST}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USER}
      SMTP_PASS: ${SMTP_PASS}
      MAIL_FROM: ${MAIL_FROM}
    depends_on:
      postgresql:
        condition: service_healthy
    deploy:
      resources:
        limits:
          memory: 300M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # BASE DE DONNÉES
  postgresql:
    image: postgres:16-alpine
    container_name: acd-postgresql
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - postgresql_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
    deploy:
      resources:
        limits:
          memory: 150M # Réduit un peu pour laisser de la place au reste

  # GÉNÉRATEUR PDF
  gotenberg:
    image: gotenberg/gotenberg:8
    container_name: acd-gotenberg
    restart: unless-stopped
    command:
      - "gotenberg"
      - "--api-timeout=60s"
      - "--chromium-start-timeout=60s"
    deploy:
      resources:
        limits:
          memory: 512M

volumes:
  postgresql_data:
  caddy_data:
  caddy_config:
