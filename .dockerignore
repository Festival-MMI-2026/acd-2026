node_modules
# .output is built by the CI before "docker build" and is COPYed into the
# image — it must NOT be excluded from the build context.
.nuxt
.git
.gitignore
*.log
.env
.env.*
!.env.example

# Tests
test
tests
playwright.config.ts
vitest.config.ts

# Divers
README.md
*.csv
